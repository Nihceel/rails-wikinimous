<ul>
  <% @articles.each do |article| %>
    <li> <h2><%= link_to article.title, articles_path(article) %></h2> </li>
  <% end %>
</ul>

<%= link_to "Delete #{@article.title}",
            article_path(@article),
            method: :delete,
            data: { confirm: "Are you sure?" } %>
